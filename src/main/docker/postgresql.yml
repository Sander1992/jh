version: '2'
services:
    test_project-postgresql:
        image: postgres:9.6.5
        # volumes:
        #     - ~/volumes/jhipster/test_project/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=test_project
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
